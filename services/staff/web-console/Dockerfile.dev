# syntax=docker/dockerfile:1.4
ARG NODE_IMAGE_VERSION=22-bookworm

FROM node:${NODE_IMAGE_VERSION}

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ENV NODE_ENV=development

EXPOSE 5173

CMD ["sh", "-ec", "exec npm run dev -- --host 0.0.0.0 --port 5173"]

