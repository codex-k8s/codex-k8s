{
  "callback_ack_received": "Response received",
  "callback_ack_unavailable": "This request is no longer available",
  "free_text_received": "Text response received",
  "free_text_unavailable": "Could not match this message to an active request",
  "free_text_failed": "Could not forward this text response. Please try again later.",
  "follow_up_applied_response": "Response accepted. {{if .RunURL}}Run: {{.RunURL}}{{end}}",
  "follow_up_edit_failed": "Response accepted. The original message could not be updated, so this confirmation was sent separately. {{if .RunURL}}Run: {{.RunURL}}{{end}}",
  "follow_up_expired_wait": "The response window has expired. {{if .RunURL}}Run: {{.RunURL}}{{end}}",
  "follow_up_operator_fallback": "Manual operator attention is required. {{if .IssueURL}}Issue: {{.IssueURL}}{{else if .RunURL}}Run: {{.RunURL}}{{end}}"
}
