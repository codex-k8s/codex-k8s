<div align="center">
  <img src="docs/media/logo.png" alt="codex-k8s logo" width="120" height="120" />
  <h1>codex-k8s</h1>
  <p>🧠 Webhook-driven платформа управления AI-агентами в Kubernetes для полного цикла: от идеи и документации до кода, PR, деплоя и эксплуатации.</p>
</div>

![Go Version](https://img.shields.io/github/go-mod/go-version/codex-k8s/codex-k8s)
[![Go Reference](https://pkg.go.dev/badge/github.com/codex-k8s/codex-k8s.svg)](https://pkg.go.dev/github.com/codex-k8s/codex-k8s)

`codex-k8s` запускает агентные роли по GitHub-лейблам (`run:*`) и ведет задачу по stage-модели:
`intake -> vision -> prd -> arch -> design -> plan -> dev -> qa -> release -> postdeploy -> ops`.

Ключевые функции платформы:
- 🚀 запуск агентных job в Kubernetes (code-only/full-env);
- 🧾 управление stage-процессом через issue labels;
- 🛡️ review gate (`state:*`, `need:*`) и аудит `flow_events`;
- 🔧 сборка/деплой и наблюдаемость через staff web-console;
- 📦 работа с конфигами/секретами, repo preflight, runtime diagnostics.

## 👥 Роли агентов и как запускать

Системные роли:
- `pm` — продуктовая проработка и формализация требований;
- `sa` — архитектура и контракты;
- `em` — планирование, delivery и quality-gates;
- `dev` — реализация и PR;
- `reviewer` — pre-review и технические замечания;
- `qa` — тестовые сценарии и регресс;
- `sre` — эксплуатация, надежность, postdeploy/ops;
- `km` — документация, трассируемость, self-improve.

Запуск выполняется постановкой `run:*` лейбла на Issue:
- `run:intake`, `run:vision`, `run:prd`, `run:arch`, `run:design`, `run:plan`, `run:dev`, `run:qa`, `run:release`, `run:postdeploy`, `run:ops`, `run:self-improve`, `run:rethink`.
- Для доработок по замечаниям Owner используйте `run:*:revise` (например, `run:dev:revise`).

Служебные лейблы:
- `state:*` — статус этапа (`state:in-review`, `state:approved` и т.д.);
- `need:*` — запрос участия роли (`need:qa`, `need:sa`, `need:reviewer` и т.д.);
- `run:debug` — сохраняет namespace после фейла/завершения для отладки.

## 🧭 Юз-кейс 1: существующий проект, новая фича (новый сервис + фронт)

1. Создайте Issue с шаблоном:
- 🎯 цель фичи;
- ✅ критерии приемки;
- 🔌 какие backend/API изменения нужны;
- 🖥️ какие изменения фронта нужны;
- 🧪 что считать успешным тестом.

2. Последовательный запуск по stage-лейблам:
- `run:design` — зафиксировать API/data model/UI-flow;
- `run:plan` — получить декомпозицию на задачи и DoD;
- `run:dev` — реализация кода, тестов и docs update.

3. После появления PR:
- платформа переводит в `state:in-review`;
- `reviewer` делает pre-review;
- Owner оставляет замечания.

4. Доработка по замечаниям:
- поставьте `run:dev:revise`;
- агент обязан пройти все открытые комментарии, ответить на каждый и запушить правки в тот же PR.

5. Тестирование и регресс:
- `run:qa` — тест-артефакты, проверка критических сценариев, фиксация рисков.

6. Слияние и выкладка:
- после approve и merge запускайте `run:release`;
- затем `run:postdeploy` и `run:ops` для проверки эксплуатационных метрик/алертов.

7. Мониторинг:
- смотрите статусы и логи в `Operations -> Runs` и `Operations -> Build & Deploy`;
- при необходимости включайте `run:debug` и анализируйте namespace в кластере.

## 🧭 Юз-кейс 2: новый пустой проект — от идеи до кода

1. Создайте проект и репозиторий в staff UI, выполните `Repository preflight`.

2. Создайте корневой Issue-идею:
- 💡 проблема и гипотеза ценности;
- 👥 целевые пользователи;
- 📊 метрики успеха;
- ⚠️ ограничения и риски.

3. Пройдите stage-цепочку:
- `run:intake` -> `run:vision` -> `run:prd` -> `run:arch` -> `run:design` -> `run:plan`.

4. На каждом этапе:
- Owner ревьюит артефакты;
- при замечаниях запускайте соответствующий `run:<stage>:revise`;
- при смене направления используйте `run:rethink`.

5. После утвержденного `run:plan`:
- запустите `run:dev` для первого инкремента кода;
- при комментариях к PR запускайте `run:dev:revise`.

6. Завершение инкремента:
- `run:qa` -> `run:release` -> merge -> deploy -> `run:postdeploy` -> `run:ops`.

7. Непрерывное улучшение процесса:
- периодически запускайте `run:self-improve` для улучшения prompts/docs/tooling по evidence из реальных run.

---

## ⚠️ Ниже — разделы, требующие доработки

Структура репозитория и остальные разделы README временно отсечены и будут переписаны в следующей итерации актуализации документации.
